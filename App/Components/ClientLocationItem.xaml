<UserControl x:Class="MaVideotheque.Components.ClientLocationItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MaVideotheque.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Content="{Binding FilmId}"/>
        <Label Grid.Column="1" Content="{Binding FilmName}"/>
        <Label Grid.Column="2" Content="{Binding LocationStart}"/>
        <Label Grid.Column="3" Content="{Binding LocationEnd}"/>
        <Label Grid.Column="4" Content="{Binding Etat}"/>
    </Grid>
</UserControl>
