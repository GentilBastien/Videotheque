<UserControl x:Class="MaVideotheque.Components.LocationItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MaVideotheque.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.45*" />
            <ColumnDefinition Width="1.7*" />
            <ColumnDefinition Width="0.8*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="0.5*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Content="{Binding NumLocation}"/>
        <Label Grid.Column="1" Content="{Binding Titre}"/>
        <Label Grid.Column="2" Content="{Binding NomClient}"/>
        <Label Grid.Column="3" Content="{Binding LocStart}"/>
        <Label Grid.Column="4" Content="{Binding LocEnd}"/>
        <Label Grid.Column="5" Content="{Binding Prix}"/>
        <Label Grid.Column="6" Content="{Binding Etat}"/>
    </Grid>
</UserControl>
